		<!-- generate breadcrumb trail using acts_as_tree: ancestors.reverse -->
		<style type="text/css">
			#breadcrumb { color:black; font-weight:bold; } /* not working as expected */
			a#homelink:hover { color:orange } 
		</style>
		<div id="breadcrumb">
			<%= link_to("Home Page",{ :controller => 'home',:action =>'index'}, :id =>"homeLink" ) %><span>&nbsp;&nbsp;&gt;&nbsp;</span>
			<% for category in @category.ancestors.reverse %>
				<%= link_to(category.name,{:controller => 'categories', :action => 'show',:id=>category.id}) %>&nbsp;&nbsp;&gt;&nbsp; <!-- these will be the intermediate categories -->
			<% end %>	
			<%= link_to(@category.name,{:controller=>'categories',:action=>'show', :id=>@category.id}) %> <!-- if you're on a product page this will be the immediately containing leaf-level category -->

		</div>
